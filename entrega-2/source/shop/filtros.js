// const divFiltros = document.getElementById('filtros')

// const labels = []

// const fillLabels = (discos) => {
//   labels.splice(0)
//   for (const disco of discos) {
//     if (!labels.includes(disco.labels)) {
//       labels.push(disco.label)
//     }
//   }
// }

// fillLabels(discos)

// const createLabelFilter = () => {
//   const divFiltros = document.querySelector('#filtros')
//   const labelFilter = document.createElement('select')

//   for (const disco of discos) {
//     const option = document.createElement('option')

//     option.value = disco.label
//     option.textContent = disco.label

//     labelFilter.appendChild(option)
//   }
//   divFiltros.appendChild(labelFilter)
// }

// createLabelFilter()
